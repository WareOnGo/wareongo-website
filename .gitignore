# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# vite-react-ssg's intermediate build output. Written to a random-hashed subdir
# during `vite-react-ssg build` and deleted when it finishes, so a `git add .`
# that races the build otherwise sweeps in the whole thing.
.vite-react-ssg-temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
